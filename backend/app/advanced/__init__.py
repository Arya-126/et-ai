"""Advanced research-grade defense capabilities.

The ALGORITHMS here are real (Schnorr ZKP, FedAvg, rPPG signal analysis, SIP
anomaly detection, an LLM scam-baiting agent). The live data sources that need
infrastructure we don't have in a hackathon — live VoIP packet capture, live
camera/audio, real multi-organisation PII, a production PKI — are simulated and
disclosed (see docs/synthetic-disclosure.md). On real inputs the same code runs
unchanged.
"""
