fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Orchestration
langgraph==0.2.28

# LLM (local, free)
ollama==0.3.3

# Graph
neo4j==5.24.0
networkx==3.3
scipy>=1.13.1            # >=1.14 auto-selected on Python 3.13 (no 1.13 wheels there)
python-louvain==0.16

# PDF
reportlab==4.2.2

# Counterfeit-currency CV (Component 2).
# OpenCV powers the always-on feature checks. torch/torchvision (the CNN) are
# installed SEPARATELY from the CPU wheel index — they're optional: without them
# the scanner runs in features-only mode (see cv/infer.py). Install with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
opencv-python-headless>=4.11,<4.14

# File uploads (currency scan)
python-multipart==0.0.9

# HTTP for demo_inject
requests==2.32.3

# Tests
pytest==8.3.3
httpx==0.27.2
